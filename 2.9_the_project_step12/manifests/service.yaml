apiVersion: v1
kind: Service
metadata:
  name: project-step12
  namespace: project
spec:
  type: ClusterIP
  selector:
    name: project-step12
  ports:
    - name: http
      port: 1234
      targetPort: 80
