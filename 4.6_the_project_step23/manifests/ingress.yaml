apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: project-todo
  namespace: project
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: project-todo
                port:
                  number: 1234
