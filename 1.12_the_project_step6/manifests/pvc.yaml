apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: project-step6-pvc
spec:
  storageClassName: project-step6-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi