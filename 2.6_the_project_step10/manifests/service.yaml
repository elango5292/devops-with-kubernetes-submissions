apiVersion: v1
kind: Service
metadata:
  name: project-step10
  namespace: project
spec:
  type: ClusterIP
  selector:
    name: project-step10
  ports:
    - name: http
      port: 1234
      targetPort: 80
