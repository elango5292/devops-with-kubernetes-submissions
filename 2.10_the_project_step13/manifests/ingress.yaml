apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: project-step13
  namespace: project
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: project-step13
                port:
                  number: 1234
