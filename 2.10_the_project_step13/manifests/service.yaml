apiVersion: v1
kind: Service
metadata:
  name: project-step13
  namespace: project
spec:
  type: ClusterIP
  selector:
    name: project-step13
  ports:
    - name: http
      port: 1234
      targetPort: 80
