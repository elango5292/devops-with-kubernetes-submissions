apiVersion: v1
kind: Service
metadata:
  name: project-step9
  namespace: project
spec:
  type: ClusterIP
  selector:
    name: project-step9
  ports:
    - name: http
      port: 1234
      targetPort: 80
