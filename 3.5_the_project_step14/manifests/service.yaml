apiVersion: v1
kind: Service
metadata:
  name: project-step14
  namespace: project
spec:
  type: ClusterIP
  selector:
    name: project-step14
  ports:
    - name: http
      port: 1234
      targetPort: 80
