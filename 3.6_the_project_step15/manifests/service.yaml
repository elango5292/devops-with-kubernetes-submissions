apiVersion: v1
kind: Service
metadata:
  name: project-step15
  namespace: project
spec:
  type: ClusterIP
  selector:
    name: project-step15
  ports:
    - name: http
      port: 1234
      targetPort: 80
