apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: project-step5
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: project-step5
                port: 
                  number: 1234